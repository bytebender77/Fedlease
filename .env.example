# Copy this file to ".env" and fill in your token. The repo's .gitignore
# (and run.py) will pick it up automatically. Never commit your real .env.
#
# Get a token at: https://huggingface.co/settings/tokens
HF_TOKEN=

# Optional — silence the symlink warning on Windows
HF_HUB_DISABLE_SYMLINKS_WARNING=1
